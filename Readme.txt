Information

This is Windows registry entries for creating a profile for Microsoft Edge browser.

It tries to limit settings to make the browser more privacy focused.

Additional settings include specifiying the Home page, new tab page, secure DNS (set to Cloudflare DNS).
These can all be amended to your own liking.